class LinkedHeatMap{
    margin = {
        top: 10, right: 10, bottom: 50, left: 20
    }

    constructor(svg, width=1200, height=600){
        this.svg = svg;
        this.width = width;
        this.height = height;
    }

    initialize(){
        
    }
}